<local:ChartDemoViewBase x:Class="Demo.Views.FinancialChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:chart="http://schemas.cheari.com/controls"
             xmlns:local="clr-namespace:Demo.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <chart:Chart x:Name="DemoChart" Grid.Row="0"
                     Background="#0D141E" BorderBrush="#2A3A50" BorderThickness="0"
                     PlotAreaBackground="#060A10" PlotAreaBorderBrush="#2A3A50" PlotAreaBorderThickness="0"
                     XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" Series="{Binding Series}"
                     Legend="{Binding Legend}"
                     Modifiers="{Binding Modifiers}" EnableAntialiasing="{Binding EnableAntialiasing}"
                     ShowFps="True" />
        <Border Grid.Row="1" Background="#162230" BorderBrush="#2A3A50" BorderThickness="0,1,0,0">
            <Grid Margin="12,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Foreground="#C8D6E5" VerticalAlignment="Center" Text="{Binding Description}" TextWrapping="Wrap" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                    <CheckBox Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#C8D6E5" Content="抗锯齿" IsChecked="{Binding EnableAntialiasing}" />
                    <TextBlock Margin="20,0,0,0" Foreground="#00E676" VerticalAlignment="Center"
                               Text="{Binding ElementName=DemoChart, Path=Fps, StringFormat=FPS:{0:F1}}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</local:ChartDemoViewBase>
