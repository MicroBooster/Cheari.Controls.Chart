<local:ChartDemoViewBase x:Class="Demo.Views.ThemeCustomizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:chart="http://schemas.cheari.com/controls"
             xmlns:local="clr-namespace:Demo.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <chart:Chart x:Name="DemoChart" Grid.Row="0"
                     Background="#05101A" BorderBrush="#1A3A5C" BorderThickness="0"
                     PlotAreaBackground="#020810" PlotAreaBorderBrush="#0F4470" PlotAreaBorderThickness="0"
                     XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" Series="{Binding Series}"
                     Modifiers="{Binding Modifiers}" EnableAntialiasing="{Binding EnableAntialiasing}"
                     ShowFps="True" />
        <Border Grid.Row="1" Background="#0F2940" BorderBrush="#1A3A5C" BorderThickness="0,1,0,0">
            <Grid Margin="12,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Background="#00DCDC" CornerRadius="6" Padding="8,3" Margin="0,0,12,0">
                        <TextBlock Foreground="#05101A" FontWeight="SemiBold" Text="{Binding ThemeName}" />
                    </Border>
                    <TextBlock Foreground="#80B8D6" VerticalAlignment="Center" Text="{Binding Description}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                    <CheckBox Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#80B8D6" Content="抗锯齿" IsChecked="{Binding EnableAntialiasing}" />
                    <TextBlock Margin="20,0,0,0" Foreground="#00E5FF" VerticalAlignment="Center"
                               Text="{Binding ElementName=DemoChart, Path=Fps, StringFormat=FPS:{0:F1}}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</local:ChartDemoViewBase>
