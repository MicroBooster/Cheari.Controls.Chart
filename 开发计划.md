# Cheari.Controls.Chart 完整开发计划与技术路线

> **最后更新**：2026-04-28
>
> **当前进度**：阶段 0 ✅ | 阶段 1 ✅ | 阶段 2 ✅ | 阶段 3~9 ❌ | 阶段 10 🔄
>
> **本次迭代重大更新**：完成了 6 阶段架构重构，引入 RingBuffer、IRenderCommand、IDownsamplingStrategy、ICoordinateMapper、IRenderContext 等接口，数据层和渲染层实现完全解耦。

## 1. 项目概述

Cheari.Controls.Chart 是一个基于 DirectX 11 的高性能 WPF 图表控件，采用标准 WPF 控件架构，支持数据绑定和 MVVM 设计模式，目标是在 .NET 10 WPF 应用中提供实时、大规模、多通道数据可视化能力。

**设计原则**：Chart 控件本身不采用 MVVM 框架实现，而是提供标准 WPF 依赖属性和接口契约，天然支持在 MVVM 框架的 WPF 应用中使用。用户可通过数据绑定方式将 ViewModel 中的数据绑定到图表，也可以自定义实现系列、轴、标注等接口以获得完全的 MVVM 体验。

### 1.1 核心性能指标
- 通道数：32 通道
- 采样率：65536 Hz / 通道
- 渲染帧率：≥ 60 FPS
- 内存占用：< 2 GB（使用环形缓冲区和浮点优化）

### 1.2 功能特性

#### 核心功能
- ✅ 折线图渲染
- ❌ 多种系列类型：散点图、柱状图、面积图、蜡烛图（OHLC）
- ❌ 多坐标轴（线性/对数），轴组共享支持
- ✅ 动态数据追加、删除、修改
- ✅ 智能降采样（Min-Max + 极值保留）
- ❌ 图例（支持曲线可见性切换）、动态标注（文本/矩形/线条）
- ✅ 丰富的鼠标交互（平移、缩放）
- ❌ 框选、悬停提示、十字光标
- ❌ 多图表共用 X 轴、布局对齐
- 🔄 完全样式化与主题切换（深色/浅色/自定义）
- ✅ 设备丢失恢复与资源管理
- ✅ 数据类型的泛型支持（float、double等）

#### 线条与标记样式
- ❌ 多种线条样式（实线、虚线、点线、点划线）
- ✅ 线条宽度自定义
- ❌ 多种标记点类型（圆形、方形、菱形、三角形）
- ❌ 区域填充样式（单色、渐变）

#### 坐标轴增强
- ❌ 时间轴支持（DateTime 坐标轴）
- ❌ 自动范围计算（NiceRange 算法）
- ❌ 主/次网格线控制
- ❌ 网格线样式自定义
- ❌ 轴标签格式器（Formatter）

#### 交互增强
- ❌ 触控手势支持（Pinch to Zoom 双指缩放）
- ✅ 鼠标滚轮缩放（以鼠标位置为中心）
- ❌ 键盘快捷键（左右键平移、+/-缩放、Home重置）
- ❌ 选中区域高亮

#### 数据功能
- ❌ 数据导出（CSV、JSON、PNG、SVG）
- ❌ 数据导入（CSV、JSON 文件加载历史数据）
- ❌ 剪贴板支持（复制选中区域数据）
- ❌ 打印和打印预览

#### 图表增强
- ❌ 堆叠图支持（堆积面积图、堆叠柱状图）
- ❌ 趋势线/拟合线（线性或多项式拟合）

## 2. 技术选型
| 组件 | 选型 | 理由 |
|------|------|------|
| 图形 API | DirectX 11 | Windows 原生，驱动成熟，兼容性好，性能稳定 |
| .NET 绑定库 | Vortice.Windows | 活跃度高，API 清晰，支持 DirectX 11/12，提供 WPF 互操作示例 |
| WPF 互操作 | D3DImage | 官方标准，可共享 DirectX 纹理 |
| 降采样算法 | Min‑Max + 自适应 LOD | 保留极值，满足高动态信号需求 |
| 数据存储 | 环形缓冲区 (RingBuffer<T>) + 批量追加 | 高效管理实时流数据，O(1) 分摊追加 |
| 渲染循环 | CompositionTarget.Rendering | 与 WPF 动画周期同步，简化集成 |
| 依赖属性 | 标准 WPF 依赖属性 | 支持数据绑定、样式、动画 |
| 样式/主题 | ResourceDictionary + Theme 枚举 | 遵循 WPF 样式系统，易于扩展 |
| 单元测试 | xUnit + BenchmarkDotNet | 确保性能稳定，检测回归 |
| 软件回退 | GdiRenderer (WriteableBitmap) | D3D11 失败时自动降级 |

## 3. 架构设计

### 3.1 分层架构

1. **表现层**
   - Chart 控件（继承自 Control）
   - 轴、图例、标注等 WPF 元素
   - 交互修饰符（平移、缩放等）

2. **数据接口层（供数据绑定使用）**
   - Chart 提供标准 WPF 依赖属性（Series, XRange, YRange 等）支持数据绑定
   - IDataSeries 及其实现，纯数据定义，支持 INotifyCollectionChanged 和 DataSeriesChangeEventArgs
   - IRenderableSeries 及其实现，渲染属性 + 数据引用
   - IAxis 及其实现，供用户实现自定义坐标轴
   - IAnnotation 及其实现，供用户实现自定义标注
   - ILegend 及其实现，供用户实现自定义图例
   - IAxisGroup 及其实现，供用户实现多图表联动
   - **说明**：以上接口契约是提供给用户绑定数据使用的，控件本身不强制要求用户使用这些接口，也支持直接通过依赖属性绑定数据

3. **渲染层**
   - IRenderer 接口（抽象渲染器）
   - D3D11Renderer 实现（GPU 渲染）
   - GdiRenderer 实现（软件回退）
   - 降采样算法（IDownsamplingStrategy，可插拔）
   - 坐标映射（ICoordinateMapper，可插拔）
   - 渲染命令（IRenderCommand，解耦图表类型）

4. **数据层**
   - 环形缓冲区 (RingBuffer<T>)：O(1) 分摊追加
   - 数据转换与处理
   - 批量数据追加接口
   - 细粒度变更通知（DataSeriesChangeEventArgs）

### 3.2 核心类图

- **Chart**：继承 Control，是控件的外观与逻辑容器。依赖属性：`ObservableCollection<IRenderableSeries> Series`, Axes, Annotations, Legend, Modifiers, Theme 等。支持标准 WPF 数据绑定。XRange/YRange 为唯一真实源（Single Source of Truth）。
- **IDataSeries<TY>**：数据系列基接口，继承 INotifyCollectionChanged，支持集合变化通知和 DataSeriesChangeEventArgs 细粒度变更通知。
  - **IUniformDataSeries<TY>**：均匀采样数据系列，通过 XSelector(int index) 计算 X 值（如 X = XStart + XStep × index），大幅节省内存。
  - **IVariableDataSeries<TX, TY>**：可变间距数据系列，提供 XValues[]、YValues[] 数组，支持任意 X 类型（double、DateTime、TimeSpan 等）。
- **IRenderableSeries**：可渲染系列接口，包含渲染属性（Stroke、StrokeThickness、IsVisible），并包含 DataSeries 属性引用实际数据。支持数据和渲染属性的分离，同一数据可用不同系列类型（折线/散点）展示。
- **LineRenderableSeries**：折线系列实现。
- **ScatterRenderableSeries**：散点系列实现（待实现）。
- **IRenderCommand**：渲染命令接口，标记接口，用于解耦图表类型与渲染器。实现类包括 LineRenderOperation。
- **IDownsamplingStrategy**：降采样策略接口，支持可插拔算法（Min-Max、LTTB、Douglas-Peucker 等）。
- **ICoordinateMapper**：坐标映射策略接口，支持可插拔映射（线性、对数、时间轴等）。
- **IRenderContext**：渲染上下文接口，解耦修饰器与具体 ChartRenderContext 实现。
- **IAxis**：轴接口契约，支持 Scale（线性/对数/时间），Min/Max，Title。供用户实现自定义坐标轴。
- **IAxisGroup**：轴组接口契约，用于多 Chart 同步范围。
- **IAnnotation**：标注接口契约（文本、矩形、线等）。供用户实现自定义标注。
- **ILegend**：图例接口契约，管理图例项，支持位置/样式。供用户实现自定义图例。
- **IChartModifier**：交互修饰符契约（平移、缩放、框选、悬停等）。
- **IRenderer**：渲染器抽象接口，解耦 DirectX 细节，支持 D3D11Renderer 和 GdiRenderer 两种实现。

### 3.3 数据接口定义

```csharp
// 数据系列基接口
public interface IDataSeries<TY> : INotifyCollectionChanged
{
    int Count { get; }
    int Version { get; }                          // 版本号，用于渲染缓存失效判断
    TY[] YValues { get; }
    void Append(TY y);                              // 单数据点追加
    void Append(IEnumerable<TY> yValues);            // 批量追加
    event EventHandler<DataSeriesChangeEventArgs>? DataChanged;  // 细粒度变更通知
}

// 均匀采样数据系列
public interface IUniformDataSeries<TY> : IDataSeries<TY>
{
    Func<int, TY> XSelector { get; }               // 通过索引计算 X 值
    double GetX(int index);                        // X = XSelector(index)
}

// 可变间距数据系列
public interface IVariableDataSeries<TX, TY> : IDataSeries<TY>
{
    TX[] XValues { get; }
    void Append(TX x, TY y);                                    // 单数据点追加
    void Append(IEnumerable<TX> xValues, IEnumerable<TY> yValues); // 批量追加
}

// 数据变更事件参数
public class DataSeriesChangeEventArgs : EventArgs
{
    public DataSeriesChangeType ChangeType { get; init; }  // Append / Replace / Reset
    public int StartIndex { get; init; }
    public int Count { get; init; }
}
```

### 3.4 渲染命令与策略接口

```csharp
// 渲染命令接口（标记接口）
public interface IRenderCommand
{
}

// 降采样策略接口
public interface IDownsamplingStrategy
{
    void Downsample(IDataSeries dataSeries, int targetCount, DataRange visibleXRange,
        List<double> sampledX, List<double> sampledY);
}

// 坐标映射策略接口
public interface ICoordinateMapper
{
    double DataToScreen(double dataValue, DataRange dataRange, double viewportSize);
    double ScreenToData(double screenValue, DataRange dataRange, double viewportSize);
}

// 渲染上下文接口（解耦修饰器）
public interface IRenderContext
{
    DataRange XRange { get; }
    DataRange YRange { get; }
    int ViewportWidth { get; }
    int ViewportHeight { get; }
    double DpiScaleX { get; }
    double DpiScaleY { get; }
    IInputElement? InputElement { get; }
    void SetRange(DataRange xRange, DataRange yRange);
    double ScreenToDataX(double screenX, int width);
    double ScreenToDataY(double screenY, int height);
}
```

### 3.5 数据流

1. **数据注入**：后台线程产生数据 → 调用 IDataSeries<T>.Append（单点或批量）→ 触发 CollectionChanged 和 DataChanged。
2. **脏标记**：Chart 监听到变化后，通过 IDataSeries.Version 变化标记渲染缓存失效。
3. **渲染循环**：CompositionTarget.Rendering 触发 → 检查渲染缓存（XRange, YRange, Viewport, DataVersion）→ 重新计算降采样 → 更新顶点缓冲区 → 调用 IRenderer.Render → D3DImage 刷新。
4. **交互**：用户鼠标/触控操作 → IChartModifier 通过 IRenderContext.SetRange 修改范围 → Chart 通过 OnRangeChanged 回调接收 → 触发属性变更 → 重新渲染。

**数据绑定说明**：Chart 控件支持两种数据绑定方式：
- **依赖属性直接绑定**：用户可将 `ObservableCollection<IRenderableSeries>` 直接绑定到 Series 依赖属性。
- **接口契约绑定**：用户可实现 IDataSeries 等接口，获得更精细的控制（如动态数据更新、样式定制等）

**数据存储优化**：
- 可变间距系列：使用 RingBuffer<T> 存储，O(1) 分摊追加
- 均匀采样系列：仅存储 YValues[]，X 通过 XSelector 计算（内存节省 50%）
- 同一 IDataSeries 可被多个 IRenderableSeries 引用（数据复用）
- IRenderableSeries 不关心数据类型，只需要 IDataSeries 接口，具体数据解析由渲染器处理

**架构解耦设计**：
- 渲染器与图表类型解耦：通过 IRenderCommand 接口，添加新图表类型无需修改渲染器
- 采样算法可插拔：通过 IDownsamplingStrategy 接口，可替换为 LTTB、Douglas-Peucker 等算法
- 坐标映射可替换：通过 ICoordinateMapper 接口，支持线性、对数、时间轴等映射
- 修饰器与上下文解耦：通过 IRenderContext 接口，便于单元测试
- 软件回退：通过 GdiRenderer 在 D3D11 失败时自动降级

## 4. 开发路线图（16 周）

| 阶段 | 名称 | 主要任务 | 输出物 | 周期 | 状态 |
|------|------|----------|--------|------|------|
| 0 | 基础设施 | - 创建 .NET 10 WPF 项目，安装 Vortice<br>- 实现 D3D11Renderer（设备、交换链、纹理共享）<br>- 处理设备丢失恢复 | 空白图表（可改变背景色） | 1 周 | ✅ 已完成 |
| 1 | 核心渲染 + 数据绑定支持 | - 定义 IDataSeries、IUniformDataSeries、IVariableDataSeries<br>- 定义 IRenderableSeries<br>- 实现数据系列类和渲染系列类<br>- 实现 Chart 控件的 ObservableCollection<IRenderableSeries> 绑定<br>- 折线图渲染，基础平移/缩放<br>- 实现均匀降采样 | 单通道可交互折线图，支持数据绑定 | 2 周 | ✅ 已完成 |
| 2 | 多种系列类型 | - 扩展系列类型（散点、柱状、面积、蜡烛）<br>- 策略模式渲染分发（IRenderCommand）<br>- 柱状图/蜡烛图自动宽度计算<br>- 线条样式选择（实线/虚线/点线）<br>- 标记点类型（圆形、方形、菱形、三角形） | 五种图表类型演示 | 2 周 | ✅ 已完成 |
| 3 | 多坐标轴 | - 定义 IAxis, LinearAxis, LogAxis<br>- 轴布局系统（Left/Right/Top/Bottom）<br>- 坐标转换（线性/对数）<br>- 系列与轴的绑定<br>- 时间轴 DateTimeAxis 支持 | 支持双 Y 轴、对数轴、时间轴的图表 | 2 周 | ❌ 未开始 |
| 4 | 图例、标注与交互修饰符 | - 图例（ILegend, 可见性控制）<br>- 标注（文本/矩形/线条，静态+可编辑）<br>- 修饰符架构（IChartModifier）<br>- 实现修饰符：平移、缩放、框选、重置、悬停提示<br>- 触控手势支持（Pinch to Zoom）<br>- 鼠标滚轮缩放 | 完整交互示例 | 2 周 | 🔄 部分完成 |
| 5 | 多图联动与共用 X 轴 | - 定义 IAxisGroup，实现 SynchronizedAxis<br>- Chart 支持 XAxisGroup/YAxisGroup<br>- 自定义 StackedChartsPanel 实现等宽布局<br>- 支持隐藏 X 轴 | 垂直堆叠两个图表，共用 X 轴 | 1 周 | ❌ 未开始 |
| 6 | 动态数据 + 高性能管道 | - 实现 RingBuffer<T> ✅<br>- 批量追加接口 AppendPoints ✅<br>- 自适应降采样（Min‑Max + LOD）✅<br>- 脏标记 + 延迟重算（渲染缓存）✅<br>- 顶点缓冲动态更新 ✅<br>- SIMD 加速（Vector<T>）<br>- 多线程并行处理各通道 | 32 通道 × 65536 Hz 实时流演示（≥60 FPS） | 3 周 | 🔄 部分完成 |
| 7 | 样式、主题与自定义模板 | - Chart 基类改为 Control，提供默认模板 ✅<br>- 定义 AxisStyle、LegendStyle 等依赖属性<br>- 实现主题系统（Dark/Light/Custom）<br>- 支持用户完全重写模板<br>- 区域填充样式（单色、渐变） | 可切换主题、可重新布局的图表 | 1.5 周 | 🔄 部分完成 |
| 8 | 坐标轴增强 | - NiceRange 自动范围计算<br>- 主/次网格线控制<br>- 网格线样式自定义<br>- 轴标签格式器（Formatter） | 图表范围自动优化 | 0.5 周 | ❌ 未开始 |
| 9 | 数据导入导出 | - CSV/JSON 导出<br>- PNG/SVG 导出<br>- 打印和打印预览<br>- 剪贴板支持 | 数据导出演示 | 0.5 周 | ❌ 未开始 |
| 10 | 生产级优化与文档 | - 设备丢失全面恢复 ✅<br>- IDisposable 资源管理 ✅<br>- 编写 API 文档（XML 注释）🔄<br>- 提供 5 个完整示例项目<br>- 打包为 NuGet | 发布 v1.0 版本 | 1 周 | 🔄 进行中 |

**总计：16 周（4 个月）**

**当前完成**：约 4 周工作量（阶段 0、阶段 1 完成，阶段 2~9 未开始，阶段 4/6/7/10 部分完成）

## 5. 关键技术难点与对策

| 难点 | 风险等级 | 解决方案 | 状态 |
|------|----------|---------|------|
| D3DImage 与 WPF 的"空域"问题 | 中 | 仅将 D3DImage 作为 Image.Source，不叠加 WPF 元素于其上；轴、图例等均使用 WPF 原生元素置于绘图区之外 | ✅ 已解决 |
| 32 通道数据降采样 CPU 负载 | 高 | 使用 SIMD（Vector<T>）加速 Min‑Max 扫描；自适应 LOD（仅在可见范围小于阈值时进行精细降采样）；多线程并行处理各通道 | 🔄 部分解决 |
| 对数轴负值/零值处理 | 低 | 数据馈送前过滤 ≤0 的点；在 UI 中给出警告并禁用对数轴或自动转换为线性 | ✅ 架构就绪（ICoordinateMapper） |
| 多 Chart 同步时的频繁重绘 | 中 | 轴组变更使用节流（Throttle）或合并更新；渲染缓存机制（按通道+范围） | 🔄 架构就绪（渲染缓存已实现） |
| 内存占用超标 | 中 | 采用 float[] 存储坐标（而非 Point 结构体）；环形缓冲区固定容量；提供 UniformDataSeries 节省 X 数组内存；同一数据可被多个系列引用 | ✅ RingBuffer 已实现 |
| 设备丢失导致黑屏 | 低 | 监听 D3DImage.IsFrontBufferAvailableChanged，重新创建设备和资源，并重新加载数据 | ✅ 已解决 |
| 泛型类型的多样化支持 | 中 | 设计 IDataSeries 接口；渲染层使用泛型缓冲区；针对 double 提供高精度路径 | ✅ 已解决 |
| D3D11 无软件 fallback | 中 | 实现 GdiRenderer 基于 WriteableBitmap，在 D3D11 初始化失败时自动降级 | ✅ 已解决 |

## 6. 测试策略

### 6.1 单元测试（xUnit）
- ✅ 轴组同步逻辑
- ✅ 降采样算法正确性（极值保留）
- ❌ 坐标转换（线性/对数/时间）
- ✅ 环形缓冲区读写边界
- ✅ 泛型接口测试（float/double/DateTime）
- ❌ NiceRange 自动范围计算测试
- ✅ 数据追加测试（单点 + 批量）
- ✅ UniformDataSeries 的 X 值计算测试
- ❌ IDataSeries 被多个 IRenderableSeries 引用的数据复用测试

### 6.2 性能基准测试（BenchmarkDotNet）
- ❌ 32 通道数据注入 + 降采样耗时
- ❌ 顶点缓冲更新吞吐量
- ❌ 不同数据量下的渲染帧率
- ❌ float vs double 性能对比
- ❌ SIMD 加速效果对比
- ❌ VariableDataSeries vs UniformDataSeries 内存占用对比
- ❌ 多个 IRenderableSeries 共享同一 IDataSeries 的性能测试

### 6.3 集成/UI 测试
- ✅ 鼠标交互（平移/缩放）的正确性
- ❌ 触控手势测试（Pinch to Zoom）
- ❌ 主题切换后的视觉验证
- ❌ 多 Chart 布局对齐验证

### 6.4 压力测试
- ❌ 连续运行 24 小时（模拟 32 通道 65536 Hz 数据），监控内存泄漏和帧率下降。

## 7. 示例演示计划

| # | 示例名称 | 状态 | 说明 |
|---|---------|------|------|
| 1 | 基础折线图 | ✅ 已完成 | 绑定静态数据，展示平移/缩放 |
| 2 | 均匀采样折线图 | ✅ 已完成 | 展示 UniformDataSeries 的内存优化效果 |
| 3 | 数据复用示例 | ❌ 待实现 | 同一数据系列同时用折线和散点展示 |
| 4 | 多轴混合图表 | ❌ 待实现 | 两个 Y 轴（左线性，右对数)，两个系列分别绑定 |
| 5 | 实时多通道示波器 | 🔄 待优化 | 32 通道模拟信号，动态追加，实时显示（SIMD/多线程待实现） |
| 6 | 金融图表示例 | ❌ 待实现 | 蜡烛图 + 成交量柱状图，共用 X 轴，时间轴 |
| 7 | 主题与模板自定义 | ❌ 待实现 | 展示外观完全重写、区域填充样式 |
| 8 | 数据导入导出示例 | ❌ 待实现 | CSV 导出、打印功能 |
| 9 | 触控操作示例 | ❌ 待实现 | 双指缩放操作 |

## 8. 发布与维护

- **源码托管**：GitHub（MIT 许可证），包含完整示例和文档。
- **NuGet 包**：Cheari.Controls.Chart，依赖 Vortice.Windows 等。
- **文档站点**：GitHub Pages 自动生成 API 文档。

### 版本计划
- **v1.0（待完成）**：稳定版，支持全部计划功能。
- **v1.1**：引入 DirectX 12 后端（实验性）；堆叠图支持。
- **v1.2**：支持导出为位图/矢量图；误差线支持。
- **v1.3**：趋势线/拟合线；更多标记点类型。

---

## 附录：已完成文件清单

### 数据层（Data/）
| 文件 | 说明 | 状态 |
|------|------|------|
| `RingBuffer.cs` | 环形缓冲区，O(1) 分摊追加 | ✅ |
| `IDataSeries.cs` | 数据系列基接口，含 Version 和 DataChanged | ✅ |
| `UniformDataSeries.cs` | 均匀采样数据系列 | ✅ |
| `VariableDataSeries.cs` | 可变间距数据系列 | ✅ |
| `DataSeriesChangeEventArgs.cs` | 细粒度变更通知 | ✅ |
| `ReadOnlyListViews.cs` | 只读列表视图 | ✅ |

### 渲染层（Rendering/）
| 文件 | 说明 | 状态 |
|------|------|------|
| `IRenderer.cs` | 渲染器接口 | ✅ |
| `D3D11Renderer.cs` | D3D11 GPU 渲染器 | ✅ |
| `GdiRenderer.cs` | GDI 软件渲染器（回退） | ✅ |
| `IRenderCommand.cs` | 渲染命令接口 | ✅ |
| `LineRenderOperation.cs` | 折线渲染操作 | ✅ |
| `LineSeriesRenderer.cs` | 折线系列渲染器，含缓存逻辑 | ✅ |
| `IDownsamplingStrategy.cs` | 降采样策略接口 | ✅ |
| `MinMaxDownsamplingStrategy.cs` | Min-Max 降采样实现 | ✅ |
| `ICoordinateMapper.cs` | 坐标映射策略接口 | ✅ |
| `LinearCoordinateMapper.cs` | 线性坐标映射实现 | ✅ |
| `ChartRenderContext.cs` | 图表渲染上下文 | ✅ |
| `IRenderContext.cs` | 渲染上下文接口（解耦修饰器） | ✅ |
| `IRenderableSeries.cs` | 可渲染系列接口 | ✅ |
| `LineRenderableSeries.cs` | 折线系列实现 | ✅ |

### 修饰器（Modifiers/）
| 文件 | 说明 | 状态 |
|------|------|------|
| `IChartModifier.cs` | 修饰符接口 | ✅ |
| `PanModifier.cs` | 平移修饰符 | ✅ |
| `ZoomModifier.cs` | 缩放修饰符（鼠标滚轮，以鼠标位置为中心） | ✅ |

### 控件核心
| 文件 | 说明 | 状态 |
|------|------|------|
| `Chart.cs` | 图表控件主类 | ✅ |

### 测试
| 文件 | 说明 | 状态 |
|------|------|------|
| `DataSeriesTests.cs` | 数据系列单元测试（20 个用例） | ✅ |
